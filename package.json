{
  "name": "codex-switchboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local multi-account Codex job dashboard with isolated logins and quota failover",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --watch --env-file-if-exists=.env src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/scheduler.js && node --check src/runner.js && node --check public/app.js",
    "account:login": "node --env-file-if-exists=.env scripts/login.js",
    "setup": "node scripts/setup.js"
  },
  "dependencies": {
    "@openai/codex": "0.155.1",
    "express": "5.2.1",
    "helmet": "8.1.0"
  }
}
